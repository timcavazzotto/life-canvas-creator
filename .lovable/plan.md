

## Igualar os dois cards de preço

O segundo card (linha 199) ainda tem a classe `featured`. Remover essa classe para garantir que ambos tenham exatamente o mesmo estilo.

**`src/pages/Index.tsx`** — linha 199:
- `<div className="price-card featured">` → `<div className="price-card">`

Alteração única e direta.

